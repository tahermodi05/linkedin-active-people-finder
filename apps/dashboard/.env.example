# Example environment variables for the dashboard (Vite)
# Vite exposes variables prefixed with VITE_ to the client via import.meta.env
# Set this to your backend base URL (no trailing slash). Example value below.

VITE_BACKEND_URL=https://veriqbackend-9mx4k30f.b4a.run
